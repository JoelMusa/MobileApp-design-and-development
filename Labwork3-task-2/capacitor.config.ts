import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Labwork3-task-2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
