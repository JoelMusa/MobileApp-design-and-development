import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'simple.navigation.app.labwork.v2',
  appName: 'simple-navigation-app-labwork-2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
